# Local Video Loops

Place local exhibition video files in this folder, then reference them from `public/config/cameras.json`.

For the GitHub Pages build, paths should include the project base path, for example:

```json
"/soft_traces_triptych/media/example-loop.mp4"
```

Use only video files you have made, licensed, or have permission to exhibit. No video files are included by default.
